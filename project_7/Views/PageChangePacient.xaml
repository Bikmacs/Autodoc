<Page x:Class="project_7.Views.PageChangePacient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:project_7.Views"
      xmlns:validators="clr-namespace:project_7.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PageChangePacient"
      Background="{DynamicResource WindowBackground}">
    <Border Background="{DynamicResource WindowBackground}" 
                BorderBrush="{DynamicResource ButtonBorder}" 
                Style="{DynamicResource PageBorder}"
                Grid.ColumnSpan="2">
        <Grid Style="{DynamicResource FormGridStyle3}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Форма изменения информации о клиенте" Grid.Row="0" Grid.ColumnSpan="2" 
                        Style="{StaticResource FormTitleLabelStyle}" Foreground="{DynamicResource FontTitle}" />

            <Label Content="Фамилия:" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />
            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBox x:Name="LastNameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                    <TextBox.Text>
                        <Binding Path="AddPacient.LastName" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:TextOnlyRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=LastNameBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}" FontSize="10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <Label Content="Имя:" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />
            <StackPanel Grid.Row="2" Grid.Column="1">
                <TextBox x:Name="NameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                    <TextBox.Text>
                        <Binding Path="AddPacient.Name" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validators:TextOnlyRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=NameBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}" FontSize="10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <Label Content="Отчество:" Grid.Row="3" Grid.Column="0" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />
            <StackPanel Grid.Row="3" Grid.Column="1">
                <TextBox x:Name="MiddleNameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                    <TextBox.Text>
                        <Binding Path="AddPacient.MiddleName" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:TextOnlyRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=MiddleNameBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}" FontSize="10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <Label Content="День Рождения:" Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />
            <DatePicker x:Name="Birthday" SelectedDate="{Binding AddPacient.Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                            Grid.Row="4" Grid.Column="1" Style="{StaticResource DatePickerStyle}"/>

            <Label Content="Номер телефона:" Grid.Row="5" Grid.Column="0" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />
            <StackPanel Grid.Row="5" Grid.Column="1">
                <TextBox x:Name="PhoneBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                    <TextBox.Text>
                        <Binding Path="AddPacient.PhoneNumber" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:PhoneNumberRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=PhoneBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}" FontSize="10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>


            <Button Grid.Column="1" Grid.Row="7" Content="Изменить" Command="{Binding ChangePacBack}" Style="{DynamicResource MaterialDesignRaisedButton}"  Background="{DynamicResource ButtonBackground}"/>
            <Button Grid.Column="0" Grid.Row="7" Content="Сбросить" Style="{DynamicResource MaterialDesignRaisedButton}" Click="Button_Click_Reset" Background="{DynamicResource ButtonDanger}"/>
            <Button Content="Назад" Grid.Row="8" Grid.Column="0"  Style="{DynamicResource MaterialDesignRaisedButton}" Background="{DynamicResource ButtonBackground}" Click="Button_Click"/>

        </Grid>
    </Border>
</Page>