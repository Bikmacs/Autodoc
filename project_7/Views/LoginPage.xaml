<Page x:Class="project_7.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:project_7.Views"
      xmlns:validators="clr-namespace:project_7.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="270" d:DesignWidth="400"
      Title="LoginPage"
      Background="{DynamicResource WindowBackground}">

    <Grid Style="{StaticResource FormGridStyle4}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>


        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Авторизация" Style="{StaticResource FormTitleLabelStyle}" Foreground="{DynamicResource FontColor}"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Идентификатор" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontTitle}"/>

        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBox x:Name="idTextBox" Style="{StaticResource FormTextBoxStyle2}" Background="{DynamicResource pole}"  Foreground="{DynamicResource FontColor}">
                <TextBox.Text>
                    <Binding Path="LoginDoctor.Id" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:IdRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl Foreground="{DynamicResource Error}" FontSize="10"
                ItemsSource="{Binding ElementName=idTextBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>


        <Label Grid.Row="2" Grid.Column="0" Content="Пароль" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}" />

        <StackPanel Grid.Row="2" Grid.Column="1">
            <PasswordBox x:Name="passwordBox" PasswordChanged="PasswordBox_PasswordChanged" Style="{StaticResource FormPasswordBoxStyle}" Background="{DynamicResource pole}"/>

            <ItemsControl Foreground="{DynamicResource Error}" FontSize="10"
                ItemsSource="{Binding ElementName=passwordBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>


        <Button Grid.Column="0" Grid.Row="5" Content="Зарегистрироваться" Click="RegButton_Click" Style="{DynamicResource MaterialDesignRaisedButton}" Background="{DynamicResource ButtonBackground}"/>
        <Button Grid.Column="1" Grid.Row="5" Content="Войти" Click="LoginButton_Click" Style="{DynamicResource MaterialDesignRaisedButton}"  Background="{DynamicResource ButtonBackground}" />

    </Grid>
</Page>
    