<Page x:Class="project_7.Views.AddPacient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:project_7.Views"
      xmlns:validators="clr-namespace:project_7.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddPacient"
      Background="{DynamicResource WindowBackground}">
    <Grid Style="{StaticResource FormGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="175"/>
        </Grid.ColumnDefinitions>

        <Label Content="Имя" Style="{StaticResource LabelStyle}" Grid.Row="1"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Style="{StaticResource FormStackPanelStyle2}">
            <TextBox x:Name="AddName" Style="{StaticResource FormTextBoxStyle2}" >
                <TextBox.Text>
                    <Binding Path="AddPacient.Name" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" >
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=AddName, Path=(Validation.Errors)}" Foreground="Red" FontSize="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Label Content="Фамилия" Grid.Row="2" Style="{StaticResource LabelStyle}"/>
        <StackPanel Grid.Column="1" Grid.Row="2" Style="{StaticResource FormStackPanelStyle2}">
            <TextBox x:Name="AddLastName" Style="{StaticResource FormTextBoxStyle2}" >
                <TextBox.Text>
                    <Binding Path="AddPacient.LastName" UpdateSourceTrigger="PropertyChanged" >
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=AddLastName, Path=(Validation.Errors)}" Foreground="Red" FontSize="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Label Content="Отчество" Grid.Row="3" Style="{StaticResource LabelStyle}"/>
        <StackPanel Grid.Column="1" Grid.Row="3" Style="{StaticResource FormStackPanelStyle2}">
            <TextBox x:Name="AddMiddleName" Style="{StaticResource FormTextBoxStyle2}">
                <TextBox.Text>
                    <Binding Path="AddPacient.MiddleName" UpdateSourceTrigger="PropertyChanged" >
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=AddMiddleName, Path=(Validation.Errors)}" Foreground="Red" FontSize="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Label Content="День Рождения" Grid.Row="4" Style="{StaticResource LabelStyle}"/>
        <DatePicker SelectedDate="{Binding AddPacient.Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                    Grid.Row="4" Grid.Column="1" Style="{StaticResource DatePickerStyle}" />

        <Label Content="Номер телефона:" Style="{StaticResource LabelStyle}" Grid.Row="5"/>
        <StackPanel Grid.Column="1" Grid.Row="5" Style="{StaticResource FormStackPanelStyle2}">
            <TextBox x:Name="NumberPhone" Style="{StaticResource FormTextBoxStyle2}" >
                <TextBox.Text>
                    <Binding Path="AddPacient.PhoneNumber" UpdateSourceTrigger="PropertyChanged" >
                        <Binding.ValidationRules>
                            <validators:PhoneNumberRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=NumberPhone, Path=(Validation.Errors)}" Foreground="Red">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Button Content="Добавить" Grid.Row="6" Grid.Column="0" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Command="{Binding AddPacientViewCommand}"/>

        <Button Content="Назад" Grid.Row="6" Grid.Column="1" Command="{Binding Back}" Style="{DynamicResource MaterialDesignRaisedButton}" />

    </Grid>
</Page>