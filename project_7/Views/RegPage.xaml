<Page x:Class="project_7.Views.RegPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:project_7.Views"
      xmlns:validators="clr-namespace:project_7.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="400"
      Title="RegPage"
      Background="{DynamicResource WindowBackground}">
    <Grid Style="{StaticResource FormGridStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Label Content="Форма добавления пациента" Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource FormTitleLabelStyle}" Foreground="{DynamicResource FontTitle}"/>

        <StackPanel Grid.Row="1">
            <Label Content="Имя" Style="{StaticResource LabelStyle}"  Foreground="{DynamicResource FontColor}"/>
            <Label Content="Фамилия" Style="{StaticResource LabelStyle}"  Foreground="{DynamicResource FontColor}"/>
            <Label Content="Отчество" Style="{StaticResource LabelStyle}"  Foreground="{DynamicResource FontColor}"/>
            <Label Content="Специализация" Style="{StaticResource LabelStyle}"  Foreground="{DynamicResource FontColor}"/>
            <Label Content="Пароль" Style="{StaticResource LabelStyle}"  Foreground="{DynamicResource FontColor}"/>
            <Label Content="Подтверждение" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}"/>
            <Label Content="Идентификатор" Style="{StaticResource LabelStyle}" Foreground="{DynamicResource FontColor}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1" Style="{StaticResource ControlsStackPanelStyle}">

            <TextBox x:Name="NameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                <TextBox.Text>
                    <Binding Path="RegisterDoctors.Name" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=NameBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            
            <TextBox x:Name="LastNameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                <TextBox.Text>
                    <Binding Path="RegisterDoctors.LastName" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=LastNameBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBox x:Name="MiddleNameBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                <TextBox.Text>
                    <Binding Path="RegisterDoctors.MiddleName" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=MiddleNameBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBox x:Name="SpecBox" Style="{StaticResource FormTextBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}">
                <TextBox.Text>
                    <Binding Path="RegisterDoctors.Specialisation" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:TextOnlyRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=SpecBox, Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Foreground="{DynamicResource Error}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Style="{StaticResource FormPasswordBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}"/>
            <PasswordBox PasswordChanged="ConfirmPasswordBox_PasswordChanged" Style="{StaticResource FormPasswordBoxStyle}" Background="{DynamicResource pole}" Foreground="{DynamicResource FontColor}"/>

            <Label Content="{Binding RegisterDoctors.Id}" Style="{StaticResource LabelStyle}"/>
        </StackPanel>
        <Button Content="Войти" Grid.Column="1" Grid.Row="2" Click="Button_Login" Style="{DynamicResource MaterialDesignRaisedButton}" Background="{DynamicResource ButtonBackground}"/>
        <Button Content="Зарегистрироваться" Grid.Column="0" Grid.Row="2" Click="Button_Register" Style="{DynamicResource MaterialDesignRaisedButton}" Background="{DynamicResource ButtonBackground}"/>




    </Grid>
</Page>